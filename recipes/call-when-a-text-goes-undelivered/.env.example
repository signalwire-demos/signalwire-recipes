SIGNALWIRE_SPACE=your-space.signalwire.com
SIGNALWIRE_PROJECT_ID=your_project_id_here
SIGNALWIRE_API_TOKEN=your_api_token_here

# the project's signing key, from the Dashboard; the callback is refused
# unless SignalWire signed it with this
SIGNALWIRE_SIGNING_KEY=your_signing_key_here

# a voice-capable number you own; the fallback call comes from it
VOICE_FROM=+15550001111

# the status_callback URL you set when sending; the signature is bound to it
STATUS_URL=https://your-host.example.com/message-status

# one marker file per message id already called; a stand-in for the unique
# key your table would use
HANDLED_DIR=handled-messages

# the server listens here
PORT=8080
