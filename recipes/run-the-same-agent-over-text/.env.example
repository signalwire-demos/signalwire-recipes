SIGNALWIRE_SPACE=your-space.signalwire.com
SIGNALWIRE_PROJECT_ID=your_project_id_here
# the token needs the Chat scope as well as the usual ones
SIGNALWIRE_API_TOKEN=your_api_token_here

# the agent serves its document behind basic auth; without these the SDK
# invents a password that exists only in the running process
SWML_BASIC_AUTH_USER=recipes
SWML_BASIC_AUTH_PASSWORD=change_me

# the public base URL of the running agent (an ngrok tunnel locally). The
# code appends the agent's route and puts the credentials above into the URL,
# because the platform fetches the SWML from it server-to-server.
AGENT_PUBLIC_URL=https://abc123.ngrok-free.app

# the agent listens here
PORT=3000
