# AgentServer serves both agents behind basic auth. Without these the SDK
# generates a password that lives only in the running process. The domain's
# webhook then gets a 401, and the password changes on every restart.
SWML_BASIC_AUTH_USER=signalwire
SWML_BASIC_AUTH_PASSWORD=choose-something-long

# Local port for the server.
PORT=3000
