# What the caller hears.
GREETING=Thanks for calling Ridgeline Cycles. We are open until six.

# Backstop in seconds against a call nobody hung up (max 14400).
MAX_DURATION=300

# Local port for the Flask app.
PORT=8080
